# 画像仕様

## 1. 保持単位

- 画像は `Sake` に紐付ける
- 1酒につき 1件
- 保存項目は `imageUri: String?`

---

## 2. 取り扱い

- URIでのみ管理し、直接ファイルパスを前提にしない
- 画像が未設定でも酒保存を許可する
- 登録元は酒編集画面とする
- 永続化時はアプリ専用領域へコピーし、永続 URI として扱う
- 画像表示は S5（画像ビューア）で行う
